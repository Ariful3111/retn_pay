import 'package:flutter/material.dart';

class AgreementPartBRent extends StatelessWidget {
  const AgreementPartBRent({super.key});

  @override
  Widget build(BuildContext context) {
    return Column();
  }

  // Build payment option checkbox
}
